# ProxyAI Instructions

请编写一个独立的 Python 脚本 cyberspace_glitch.py，实现《赛博朋克 2077》风格的赛博空间视频特效。脚本必须健壮、内存可控，适合 CPU 环境运行。

硬性环境与依赖：

· Python 3.9+，仅允许 opencv-python、numpy、rembg、Pillow。
· 强制使用 CPU 模式。在脚本开头设置 os.environ["CUDA_VISIBLE_DEVICES"] = "-1" 和 cv2.setNumThreads(1) 以节省资源。
· 输入：脚本同级目录下的 input.mp4。输出：output.mp4，帧率与原片一致，编码 mp4v。

核心视觉流水线（请按以下函数模块编码）：

1. 视频加载器：使用 cv2.VideoCapture 读取。如果视频为空，自动生成 500 帧彩色测试画面（避免报错）。
2. 核心点云渲染器 render_pointcloud(frame, mask, num_points=8000)：
   · 抠图：调用 rembg.remove() 获取 mask。注意：rembg 返回的 mask 需要 resize 到原图尺寸。
   · 采样：从原图中随机采样 num_points 个像素坐标（使用 np.random.choice）。
   · 过滤：仅保留落在 mask 白色区域（人物）的点，数量不足时从背景补足。
   · 绘制：新建全黑画布。使用 cv2.rectangle 绘制每个点（尺寸 2~4 像素随机抖动）。人物点强制渲染为品红 (255,0,255)，背景点强制渲染为青蓝 (50,220,180)。（这是点云版的颜色极化）
   · 深度视差：将点分成 3 层（Z轴），每层在水平方向偏移不同像素（1~3px），制造立体感。
3. 后处理特效 apply_glitch_effects(frame, prev_frame, prev2_frame)：
   · 像素排序（竖切）：将画面垂直切成 15 个竖条。对每个竖条内部，仅在亮度高于 100 的像素上按亮度升序重排。只重排行方向，不跨列。
   · 数据混杂（Datamoshing）：随机选取画面中的 3 个矩形区域（大小 50x50~150x150），将当前帧这些区域的像素，替换为 prev2_frame（上上帧） 相同坐标的像素。这是模拟 I-frame 损坏的核心。
   · 残影混合：与 prev_frame 做指数衰减混合：result = current * 0.6 + prev * 0.3 + prev2 * 0.1。注意混合要在点云绘制完成后、输出前进行。
4. 主循环 main()：
   · 初始化 rembg 会话（只加载一次）。使用 rembg.new_session("u2net") 并传入 providers=['CPUExecutionProvider']。
   · 循环读取每一帧。处理前将帧缩放到 720p（宽度 1280，高度按比例），大幅降低 CPU 计算量（这是赛博空间“低分辨率扫描线”的美学来源）。
   · 每帧调用上述两个函数。
   · 内存保护：使用 del 显式删除上一帧的大数组，防止 rembg 残留。
   · 每处理 1 帧打印一次进度（简单起见），而不是 10 帧，以便观察卡顿位置。
   · 若处理时间超过预期，在控制台打印“处理中，请勿关闭...”。

代码质量要求：

· 脚本必须有一个 if __name__ == "__main__": main()。
· 所有 numpy 操作必须确保 dtype=np.uint8，防止 cv2.imshow 报错（虽然我们不显示，但写入需要）。
· 如果 input.mp4 不存在，脚本自动生成一个测试视频用于调试，而不是直接崩溃。

直接输出完整 Python 代码，不要额外解释。


辅助 prompt：

请用 Python 实现《赛博朋克 2077》风格的赛博空间（Cyberspace）视频风格化脚本。这是我们在 SIGGRAPH 2021 分享的技术方案的开源复现。

核心视觉理念（必须理解后实现）

我们的赛博空间视觉核心基于 稀疏点云（Sparse Point Clouds） 与 故障效果（Glitch Effects）。后期处理中使用 像素排序（Pixel Sorting） 和 数据混杂（Datamoshing） 来进一步隐藏底层几何图形，营造超现实、出神入化的体验。

技术约束：纯 CPU，输入 input.mp4，输出 output.mp4。仅允许 opencv-python、numpy、rembg。严禁第三方故障库，所有特效手写实现。

---

流水线第一阶段：点云化（Point Cloudization）—— 赛博空间的核心骨架

这是上一版本缺失但最重要的环节。对每一帧：

1. 下采样：将帧缩放到原尺寸的 30%~50%，模拟点云的稀疏感。
2. 提取点阵：从缩放后的图像中随机采样 5000~15000 个像素点，每个点记录 (x, y, r, g, b)。点的数量控制点云的“密度”——越少越像早期赛博空间，越多越接近超梦体验。
3. 点的尺寸抖动：每个点渲染为 2~5 像素的四边形（Quad），尺寸加入随机抖动，模拟摄影测量中点的位置误差。
4. 深度层叠：将点云分成 3~6 个深度层（Z-layer），每层赋予不同的水平偏移量（视差），制造立体感。层的数量对应 Houdini 中三维空间聚类的“块”。
5. 点云颜色计算：点颜色从原始图像采样，并应用 3D Noise 和阴影遮罩进行艺术化处理。

第二阶段：后处理特效（Post-Process Effects）

点云渲染完成后，叠加后处理：

1. 像素排序（Pixel Sorting）：这是赛博空间撕裂感的核心。对画面垂直等分为 12~20 个竖条，在每个竖条内按亮度升序排列像素。排序阈值建议仅对高于平均亮度的像素进行。
2. 数据混杂（Datamoshing）：故意破坏画面几何结构。实现方式：随机选取 5~10 个矩形区域，将这些区域的像素替换为前一帧同一位置的像素（I-frame 与 P-frame 的“错位”），制造“几何图形被隐藏”的超现实感。
3. 颜色极化：背景强制为冷青色（BGR: [180, 220, 50]），前景人物强制为品红色（RGB: [255, 0, 255]）。
4. 残影拖尾（帧混合）：使用指数衰减混合——当前帧(0.6) + 上一帧(0.25) + 上上一帧(0.15)，模拟点云运动时的视觉滞留。

第三阶段：额外故障注入（可选，模拟“系统故障”美学）

· Chromatic Aberration：RGB 三通道分别偏移 2~4 像素。
· Scanline 扫描线：每隔 4~8 行插入一条半透明黑线。

工程约束：

· rembg 模型只加载一次。
· 每 10 帧打印进度。
· 所有数组操作注意 dtype 转换（uint8）。
· 代码包含 main() 函数。